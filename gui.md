# SlipSnap — Предложения по улучшению UX/UI

Документ основан на анализе исходного кода: `gui.py`, `editor/editor_window.py`, `editor/ui/toolbar_factory.py`, `editor/ui/canvas.py`, `editor/ui/color_widgets.py`, `editor/ui/meme_library_dialog.py`, `design_tokens.py`.

---

## 1. Лончер (Launcher)

### 1.1 Слишком маленькие кнопки при плотном расположении
**Проблема:** Лончер фиксированного размера 330×85 px (`Metrics.LAUNCHER_WIDTH/HEIGHT`). При добавлении новых кнопок они сжимаются, иконки 20px становятся сложно попасть мышью.
**Решение:** Увеличить высоту до 96–100 px, кнопки до минимум 36×36 px. Сгруппировать редко используемые действия (серийная съёмка, видео) в выпадающее меню с иконкой «⋯».

### 1.2 Нет обратной связи при старте захвата
**Проблема:** После клика «Снимок» лончер мгновенно скрывается, пользователь не понимает, что захват начался.
**Решение:** Показать короткую анимацию исчезновения (fade-out 150 мс) или изменить курсор на перекрестие с tooltip «Выделите область» до появления оверлея.

### 1.3 Форма выделения — неочевидный переключатель
**Проблема:** Кнопка «Квадрат / Круг» переключает форму без явного визуального подтверждения текущего режима — иконка просто сменяется.
**Решение:** Добавить метку под иконкой («Прямоугольник» / «Эллипс») или рядом в одну строку, выделить активный режим цветом `PRIMARY`.

### 1.4 Отсутствует индикатор активной горячей клавиши
**Проблема:** Горячая клавиша захвата хранится в конфиге, но в самом лончере не отображается.
**Решение:** Показать текущую горячую клавишу рядом с кнопкой «Снимок» мелким текстом цвета `TEXT_MUTED`, например: `Ctrl+Alt+S`.

---

## 2. Оверлей захвата (Selection Overlay)

### 2.1 Подсказка отображается в одном месте
**Проблема:** Hint-метка фиксирована в нижнем-левом углу (`OVERLAY_HINT_OFFSET = (24, 24)`). На маленьких экранах или при выделении этого угла она может перекрываться рамкой.
**Решение:** Перемещать hint-блок динамически: если пользователь уже начал выделение — скрывать метку, или переместить её за пределы выделяемой области.

### 2.2 Нет отображения размеров выделяемой области
**Проблема:** Во время перетаскивания пользователь не знает точный размер области в пикселях.
**Решение:** Показывать рядом с курсором небольшой tooltip `«Ширина × Высота»` в реальном времени (как в Snagit / ShareX).

### 2.3 Нет увеличительного стекла (Magnifier)
**Проблема:** Сложно точно попасть на границу элемента при высоком DPI.
**Решение:** При нажатом LKM показывать небольшое окно лупы 80×80 px рядом с курсором с увеличением 3× для точного позиционирования.

---

## 3. Редактор — Верхняя панель инструментов (Actions Toolbar)

### 3.1 Кнопка цвета слишком маленькая
**Проблема:** `ColorButton` имеет размер 24×20 px (`COLOR_BUTTON_WIDTH/HEIGHT`). Это значительно меньше соседних кнопок (≥ 38×38 px), её сложно нажать.
**Решение:** Увеличить до 32×28 px минимум. Добавить рамку с тенью, чтобы кнопка выделялась среди белого фона тулбара.

### 3.2 Палитра цветов — только 10 фиксированных цветов
**Проблема:** `HexColorDialog` открывает сетку из 10 предустановленных цветов без возможности ввести произвольный HEX или выбрать через HSV-пикер.
**Решение:**
- Добавить кнопку «+» / «Другой цвет…» в нижней части палитры, открывающую стандартный `QColorDialog`.
- Добавить поле ввода HEX-кода непосредственно в попап.
- Предусмотреть 2–3 слота «пользовательских цветов» с сохранением между сессиями.

### 3.3 Нет индикатора толщины пера
**Проблема:** Толщина пера хранится в конфиге (`pen_width: 3`), но в UI нет никакого контрола для её изменения в реальном времени — приходится идти в настройки.
**Решение:** Добавить в Actions Toolbar небольшой `QSpinBox` или слайдер «Толщина» (диапазон 1–20) рядом с кнопкой цвета. Показывать превью штриха в кнопке.

### 3.4 Слайдер зума занимает много места без визуального превью
**Проблема:** Слайдер 120 px (`ZOOM_SLIDER_WIDTH`) занимает больше места, чем любая иконка в тулбаре, а рядом дублирующийся `QLabel` с процентами.
**Решение:** Заменить на компактный `QSpinBox` с суффиксом `%` (ввод с клавиатуры + колёсико мыши) шириной ≈ 64 px. Или сохранить слайдер, но убрать отдельный label — сделать значение tooltip'ом слайдера.

### 3.5 Иконки без текстовых подписей
**Проблема:** Все кнопки Actions Toolbar используют `show_text=False`, то есть только иконки. Новому пользователю не очевидно, что делает каждая кнопка.
**Решение:** Добавить опциональный режим «Показывать подписи» (можно сохранять в конфиге). По умолчанию — иконки + tooltip, но дать пользователю возможность включить подписи через меню «Вид».

---

## 4. Редактор — Левая панель инструментов (Tools Toolbar)

### 4.1 Переключение карандаш/маркер через правую кнопку мыши — неочевидно
**Проблема:** Для смены режима рисования (Pencil/Marker) нужно ПКМ по кнопке. Это не стандартное поведение, нигде не подсказано.
**Решение:** Сделать кнопку «Карандаш» разделённой (split button): левая часть активирует текущий инструмент, правая — стрелка «▾» открывает выпадающее меню. Аналогично для кнопки «Фигуры».

### 4.2 Нет предварительного просмотра инструмента на кнопке
**Проблема:** Кнопка «Блюр» всегда выглядит одинаково вне зависимости от размера кисти. Кнопка «Ластик» — тоже.
**Решение:** При изменении размера инструмента обновлять иконку кнопки (или добавить маленький индикатор размера под иконкой, например, горизонтальную полоску пропорциональной ширины).

### 4.3 Нет индикатора текущего режима для «Карандаш/Маркер»
**Проблема:** После переключения в режим «Маркер» иконка меняется, но нет текстового label'а рядом с кнопкой.
**Решение:** Показывать tooltip с текущим режимом и отображать под иконкой маленький цветной дот (`●`) или badge с буквой «M» / «P».

### 4.4 Кнопка «Мемы» визуально не отделена от инструментов рисования
**Проблема:** `memes_btn` добавляется в самом верху тулбара инструментов через `addWidget`, визуально смешиваясь с инструментами выделения/рисования.
**Решение:** Вынести «Мемы» в Actions Toolbar (горизонтальный) рядом с «OCR» и «История», где собраны утилиты. Это логично: мемы — это контент, не инструмент рисования.

---

## 5. Редактор — Холст (Canvas)

### 5.1 Нет индикатора размера изображения
**Проблема:** Пользователь не видит разрешение загруженного изображения.
**Решение:** Добавить в статус-бар (`QStatusBar`) постоянный виджет с размером изображения: `«1920 × 1080 px»`. Обновлять при изменении холста.

### 5.2 Нет индикатора позиции курсора
**Проблема:** При точной разметке координаты курсора не отображаются.
**Решение:** Добавить в статус-бар координаты курсора `«X: 240  Y: 135»`, обновляемые в реальном времени при движении мыши над холстом.

### 5.3 Нет индикатора масштаба в статус-баре
**Проблема:** Масштаб виден только в слайдере тулбара. Если пользователь изменяет зум колесом мыши — слайдер синхронизируется, но нет быстрого способа сбросить к 100%.
**Решение:** Добавить кликабельный виджет масштаба в статус-бар (`«100%»` → клик сбрасывает к 100%), аналог Figma/Photoshop.

### 5.4 Нет предварительного просмотра цвета при рисовании
**Проблема:** Текущий цвет рисования виден только в кнопке тулбара.
**Решение:** Показывать кружок с текущим цветом рядом с курсором при активных инструментах «Карандаш», «Маркер», «Фигура».

---

## 6. Инструмент «Текст»

### 6.1 Нет визуальной панели форматирования
**Проблема:** Размер шрифта хранится в конфиге (`font_px: 18`), но нет интерактивного контрола в UI.
**Решение:** При активации инструмента «Текст» показывать контекстную панель (context toolbar) прямо над холстом или в Actions Toolbar с полем размера шрифта и базовыми опциями (жирный, курсив).

### 6.2 Нет выбора шрифта
**Проблема:** Используется шрифт по умолчанию системы, у пользователя нет возможности сменить.
**Решение:** Добавить `QFontComboBox` в контекстную панель текстового инструмента.

---

## 7. Библиотека мемов (MemesDialog)

### 7.1 Тёмная тема диалога конфликтует со светлым редактором
**Проблема:** Редактор использует светлую тему (`#f8f9fa`, белые тулбары), а диалог мемов — тёмную (`#0f141a`). Переход создаёт визуальный разрыв.
**Решение:** Переключить тему диалога на светлую (соответствующую редактору) или дать пользователю глобальный переключатель темы приложения.

### 7.2 Нет предпросмотра мема перед вставкой
**Проблема:** Миниатюры 92×92 px могут быть недостаточны для распознавания содержимого.
**Решение:** При наведении на элемент показывать floating tooltip-превью размером 200×200 px с настоящим размером и именем файла.

### 7.3 Нет поддержки Drag & Drop из файлового менеджера
**Проблема:** Добавление мемов только через диалог открытия файлов.
**Решение:** Реализовать `dragEnterEvent` / `dropEvent` на `QListWidget` для добавления мемов перетаскиванием файлов извне.

### 7.4 Неочевидна разница между «Вставить» и двойным кликом
**Проблема:** Кнопка «Вставить» и двойной клик делают одно и то же — пользователь не понимает зачем обе опции.
**Решение:** Убрать кнопку «Вставить», оставить только двойной клик + Enter. Освободившееся место отдать под более полезные элементы.

---

## 8. Глобальные проблемы UX

### 8.1 Нет тёмной темы редактора
**Проблема:** Редактор полностью светлый. Лончер — тёмный. Пользователи, работающие в ночное время, не могут переключиться.
**Решение:** Добавить переключатель «Светлая / Тёмная тема» в меню «Вид» → «Тема». Соответствующие токены уже частично определены в `Palette` (есть `MEME_BACKGROUND` и другие тёмные цвета).

### 8.2 Нет экрана приветствия / быстрого справочника
**Проблема:** Новый пользователь открывает редактор и не знает о горячих клавишах (V, R, O, L, A, P, B, E, T, Ctrl+Z, Ctrl+C, Ctrl+S, Ctrl+K…).
**Решение:** Добавить меню `Помощь → Горячие клавиши` (или `?` в тулбаре), открывающее компактный диалог с таблицей всех шорткатов.

### 8.3 Нет уведомлений об успешных действиях
**Проблема:** После «Копировать» (`Ctrl+C`) или «Сохранить» пользователь не получает никакого feedback'а — лишь стандартный системный диалог сохранения.
**Решение:** Показывать в статус-баре или в виде «toast»-уведомления: `«Скопировано в буфер обмена ✓»` или `«Сохранено: snap_001.png ✓»` на 2–3 секунды.

### 8.4 Стили `transform: scale()` не поддерживаются в Qt CSS
**Проблема:** В `toolbar_factory.py` и `color_widgets.py` используются CSS-правила типа `transform: translateX(2px)` и `transform: scale(1.05)`. Qt не поддерживает CSS transform — эти правила молча игнорируются.
**Решение:** Реализовать анимации через `QPropertyAnimation` / `QGraphicsEffect`, или убрать эти правила из стилей (они не дают эффекта и захламляют код).

### 8.5 Нет индикатора очереди undo/redo
**Проблема:** Пользователь не знает, сколько шагов можно отменить.
**Решение:** Сделать tooltip кнопок Undo/Redo динамическим: `«Отменить: Нарисовал линию (3/10)»` — показывать описание последнего действия из `QUndoStack`.

### 8.6 Нет автосохранения / черновиков
**Проблема:** Если редактор закрыть случайно, все правки теряются.
**Решение:** Добавить автосохранение в `HISTORY_DIR` каждые N секунд (настраивается), с возможностью восстановления при следующем запуске.

---

## 9. Доступность (Accessibility)

### 9.1 Нет `accessibleName` у кнопок инструментов
**Проблема:** Кнопки в левом тулбаре не имеют `setAccessibleName`, что делает приложение неиспользуемым со screen reader.
**Решение:** Добавить `btn.setAccessibleName("Инструмент: Карандаш")` и т.д. для всех кнопок.

### 9.2 Контрастность некоторых элементов недостаточна
**Проблема:** `TEXT_MUTED = "#94a3b8"` на белом фоне `#ffffff` даёт контраст ~3.0:1 — ниже WCAG AA (4.5:1 для мелкого текста).
**Решение:** Заменить `TEXT_MUTED` на `#718096` (контраст ~4.6:1) для текстовых элементов.

---

## 10. Приоритет внедрения

| Приоритет | Улучшение | Сложность |
|-----------|-----------|-----------|
| 🔴 Высокий | Полноценный выбор цвета (HEX-поле + QColorDialog) | Средняя |
| 🔴 Высокий | Контрол толщины пера в тулбаре | Низкая |
| 🔴 Высокий | Toast-уведомления при копировании/сохранении | Низкая |
| 🔴 Высокий | Убрать нерабочие CSS `transform` | Низкая |
| 🟡 Средний | Размер изображения и координаты в статус-баре | Низкая |
| 🟡 Средний | Справочник горячих клавиш (`?` / меню Помощь) | Низкая |
| 🟡 Средний | Split button для карандаш/маркер и фигуры | Средняя |
| 🟡 Средний | Динамический tooltip для Undo/Redo | Низкая |
| 🟡 Средний | Предпросмотр мема при наведении | Средняя |
| 🟢 Низкий | Тёмная тема редактора | Высокая |
| 🟢 Низкий | Увеличительное стекло при захвате | Высокая |
| 🟢 Низкий | Автосохранение черновиков | Средняя |
| 🟢 Низкий | Drag & Drop в библиотеку мемов | Средняя |
